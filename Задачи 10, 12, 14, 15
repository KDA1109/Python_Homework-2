# Задача 10: На столе лежат n монеток. Некоторые из них лежат вверх решкой, а некоторые – гербом. 
# Определите минимальное число монеток, которые нужно перевернуть, чтобы все монетки были повернуты вверх одной и той же стороной. 
# Выведите минимальное количество монет, которые нужно перевернуть
# РРРОРООРОРР -> 4, ОРОРОРОРОРО -> 5

import random as rnd

n = int(input('Введите количество монеток: '))

coins = [rnd.choice([0, 1]) for coins in range(n)]

min_count = min(coins.count(0), coins.count(1))
result = sum(1 for element in coins if coins.count(element) <= min_count)
print(*coins, end=f' -> {result}')


# Задача 12: Петя и Катя – брат и сестра. Петя – студент, а Катя – школьница. Петя помогает Кате по математике. 
# Он задумывает два натуральных числа X и Y (X,Y≤1000), а Катя должна их отгадать. Для этого Петя делает две подсказки. 
# Он называет сумму этих чисел S и их произведение P. Помогите Кате отгадать задуманные Петей числа.
# 7, 10 -> 2, 5 (2 + 5 == 7; 2 * 5 == 10)

import sympy as smp
s = int(input('Сумма загаданных чисел ровна: '))
p = int(input('Произведение загаданных чисел ровно: '))

a = 1
b = -s
c = p
d = (b**2) - (4*a*c)

if d > 0:
    x = (-b - smp.sqrt(d)) / (2*a)
    y = (-b + smp.sqrt(d)) / (2*a)
elif d == 0:
    x = y = (-b) / (2*a)
else:
    print('Нет решения')

print(f"X = {x}, Y = {y}")


# Задача 14: Требуется вывести все целые степени двойки (т.е. числа вида 2k), не превосходящие числа N.
# 50 -> 1, 2, 4, 8, 16, 32

n = int(input('Введите число N: '))
k = 0

while k <= n:
    if (2 ** k > n):
        break
    else:
        print(2 ** k, end=' ')
        k += 1


# Задача №15. Иван Васильевич пришел на рынок и решил купить два арбуза: один для себя, а другой для тещи.
# Понятно, что для себя нужно выбрать арбуз потяжелей, а для тещи полегче. Но вот незадача:
# арбузов слишком много и он не знает как же выбрать самый легкий и самый тяжелый арбуз? Помогите ему!
# Пользователь вводит одно число N – количество арбузов. Вторая строка содержит N чисел, записанных на новой строчке каждое. Здесь каждое
# число – это масса соответствующего арбуза Input: 5 -> 5 1 6 5 9 Output: 1 9

n = int(input("Введите количество арбузов: "))

masses = []
for i in range(n):
    mass = int(input(f"Введите массу арбуза {i + 1}: "))
    masses.append(mass)

min_mass = min(masses)
max_mass = max(masses)

print(*masses, sep=', ')
print(f'Самый легкий арбуз весит {min_mass} кг, самый тяжёлый весит {max_mass} кг')


# Cпособ 2: используя генератор случайных чисел

from random import randint as rnd
n = int(input('Введите количество арбузов: '))

mass = [rnd(1, 15) for mass in range(n)]
min_mass = mass[0]
max_mass = mass[0]
for i in mass:
    if i > max_mass:
        max_mass = i
    elif i < min_mass:
        min_mass = i

print(*mass, sep=', ')
print(f"Самый легкий арбуз весит {min_mass} кг, самый тяжёлый весит {max_mass} кг")








